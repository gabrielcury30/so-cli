# SO CLI

## Dependencies
Compiler and tools

    GCC (GNU Compiler Collection) or Clang

    Make (build system)

## Libs

    NCurses (terminal interfaces lib)

To run the project, execute the following commands:

```bash
make
./scheduler
```
